#include <stdio.h>

int main() {
    int a, b;

    printf("Digite dois numeros: ");
    scanf("%d %d", &a, &b);

    if (a < b)
        printf("Menor: %d\n", a);
    else
        printf("Menor: %d\n", b);

    return 0;
}